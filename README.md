# ssh-bruteforce-detection-system
A Bash-based SSH security tool that monitors authentication logs, detects repeated failed login attempts, generates alerts, and automatically blocks suspicious IPv4/IPv6 addresses using iptables. Supports multiple SSH log sources, configurable thresholds, time windows, and manual IP unblocking.
